<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.edu.uniandes.fourbidden.vinilos">

    <uses-sdk android:minSdkVersion="5" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vinilos">
        <activity
            android:name=".vista.CrearTrackActivity"
            android:exported="false" />
        <activity
            android:name=".vista.CrearAlbumActivity"
            android:exported="false" />
        <activity
            android:name=".vista.AlbumActivity"
            android:exported="false" />
        <activity
            android:name=".vista.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".vista.MusicoActivity"
            android:exported="false" />
        <activity
            android:name=".vista.ColeccionistaActivity"
            android:exported="false"
            tools:ignore="MissingClass" />
    </application>

</manifest>